super + Return
  $HOME/.scripts/menu/launch-menu.sh
  
super + Home
  $HOME/.scripts/set-random-wallpaper.sh

XF86Audio{Raise,Lower}Volume
  $HOME/.scripts/vol{up,down}.sh

super + F1
  $HOME/.scripts/mute.sh

super + {F7,F8}
   brightnessctl s {4%-,+4%} && pkill -44 lemontopc

super + {1-5}
  xsetroot -name "fsignal:view ui {0-4}" && pkill -47 lemontopc

super + shift + {j,k}
  xsetroot -name "fsignal:shiftview ui {-1,1}" && pkill -47 lemontopc

super + e
  $TERMINAL -e ranger

super + shift + Escape
  killall xinit

super + R
  killall $WM

super + Tab
  rofi -show window -config /usr/share/rofi/themes/gruvbox-dark.rasi

Print
  $SCRIPTS_FOLDER/lemon/dunst-timer.sh "Taking a Screenshot" "$SCRIPTS_FOLDER/icons/camera.png" && scrot "$(date +%c).png" -e 'mv "$f" ~/Pictures/Screenshots/ ; dunstify -t 1500 -i "~/.scripts/icons/camera.png" "Screenshot Saved"'
